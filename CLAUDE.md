Claude Code

@AGENTS.md

## Claude-Specific Rules

- Use plan mode (`/plan`) before multi-file changes or when implementing a PRD task for the first time
- Use subagents for isolated tasks (e.g., "read this PRD and generate tasks", "verify this component matches the design spec")
- Run `/clear` between unrelated tasks to avoid context pollution
- When working in a department, read that department's `AGENTS.md` before touching code

I'm building a landing page by pasting self-contained sections one at a time. First, do ONLY this setup, then stop and wait.

Setup: Create a minimal React + Tailwind CSS project (use your standard setup — Vite or Next.js is fine). Nothing else: no component libraries, no global theme, no custom fonts, no extra dependencies yet. Leave the main page empty, ready for sections.

Then wait. I'll paste each section as its own prompt, one at a time. For every section I send:

Add it as a new component file with a unique name and render it in the main page after the previously added sections, in the order I send them.
Keep every previously added section exactly as-is — never edit, restyle, re-theme, or remove earlier sections.
Each section is self-contained: scope its fonts, styles, and any resets to that section only. Do not apply any global font, color, or theme — each section brings its own exact styling.
Install only the libraries that section names.

Reply with only "Ready — empty React + Tailwind project set up" and then wait for my first section. Don't add anything I didn't ask for.

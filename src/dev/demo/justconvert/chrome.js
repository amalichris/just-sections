/**
 * Page chrome for the JustConvert demo snapshot.
 *
 * Mirrors `justconvert/web/src/pages/justconvert/chrome.js` — this is a dev-only
 * copy for exercising sections against real JustConvert content, not the
 * source of truth. The live version also builds placement-tagged App Store
 * URLs for click-tracking attribution; that's product analytics wiring with
 * nothing to demonstrate here, so this copy keeps a single untagged URL.
 */

export const PRODUCT_NAME = 'JustConvert'

export const APP_STORE_URL = 'https://apps.apple.com/app/id6762390927?pt=127940636'

export const APP_STORE_BADGE_ALT = 'Download on the App Store'
